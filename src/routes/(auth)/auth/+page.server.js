export const actions = {
	login: async (event) => {
		// TODO log the user in
	},
    forgotPassword: async (event) => {
        // TODO send the user a password reset email
    },
    regTwoFA: async (event) => {
        // TODO register the user's 2FA
    },
    verifyTwoFA: async (event) => {
        // TODO verify the user's 2FA
    }
};
